data_table = "<%= j(render partial: 'data_table', locals: { results: @results, desired_metric: @desired_metric }) %>"

data_table_div = document.querySelector("#data-table")
data_table_div.innerHTML = data_table

$('#data-table > table').DataTable( {
  "lengthChange": false,
  
});
